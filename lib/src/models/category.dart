class ProductCategory {
  int id;
  String name;
  String imageUrl;
  bool isSelected;
  ProductCategory({
    required this.id,
    required this.name,
    required this.imageUrl,
    required this.isSelected,
  });
}